# Federated Registry and Exchange Guide

Operators manage registry federation links via `configs/assurance_exchange/registries.yaml`.

- Trust Domains: Internal vs External partner definitions.
- Sync Modes: Push vs Pull vs Snapshot.

Run the exchange pass:
`python -m sports_signal_bot.main assurance-exchange run-assurance-exchange-pass`

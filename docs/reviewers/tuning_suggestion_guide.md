# Tuning Suggestion Review Guide

When reviewing a tuning suggestion:
1. Check the `evidence_refs`. Does the support justify the change?
2. Review the `contradiction_burden`. Are there conflicting precedents?
3. Check the `scope`. Is the blast radius appropriately constrained?
4. Ensure the `risk` matches your expectations for the target component.

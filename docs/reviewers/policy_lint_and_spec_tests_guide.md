# Policy Lint & Spec Tests Guide

Reviewers should rely on policy linting to catch semantic errors such as shadowed rules or missing signatures before manual review.
A passed linting stage is a prerequisite for merging policy bundles.

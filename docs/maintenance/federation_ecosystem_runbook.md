# Federation Ecosystem Runbook

Routine tasks:
- Detect stale baseline catalogs (`preview-baseline-catalogs`).
- Check hub queue pressure (`preview-federation-ecosystem-health`).
- Re-evaluate participant visibility if sovereignty treaties change.

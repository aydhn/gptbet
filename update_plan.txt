1.  **Define Concurrency Contracts:** Create `contracts.py` to define the necessary Pydantic models for Concurrency Guards, Bounded Parallelism, Async Ordering, Race Probes, Shared State, Idempotency, Stale Reads, Queues, Timeout/Cancellation, and Concurrency Regressions. (Completed)
2.  **Implement Concurrency Hardening Logic:** Create modules corresponding to each contract area (e.g., `guards.py`, `parallelism.py`, `ordering.py`, `race_probes.py`, `shared_state.py`, `idempotency.py`, `stale_reads.py`, `queues.py`, `timeouts.py`, `regressions.py`) to provide helper functions for building, validating, detecting, and summarizing concurrency health and issues. (Completed)
3.  **Implement Hardening Strategies:** Create a `strategies/` directory with `base.py`, `conservative.py`, `balanced_bounded_parallelism.py`, and `async_safety_first.py` to define different levels of strictness for concurrency checks. (Completed)
4.  **Create Integration Module:** Build `integration.py` to orchestrate a full concurrency hardening pass by combining the logic from all the individual modules and producing manifests for each area, alongside an overall health report (`manifests.py`). (Completed)
5.  **Build CLI Command Line Interface:** Create `cli_concurrency_hardening.py` as a Typer app exposing commands like `run-hardening-pack-03`, `preview-concurrency-guard-report`, `preview-parallelism-report`, and `list-concurrency-hardening-strategies`. It generates JSON artifacts and prints summaries to the console. (Completed)
6.  **Integrate CLI into Main Entry Point:** Modify `src/sports_signal_bot/main.py` to include the newly created `concurrency_hardening_app` under the `concurrency` namespace. (Completed)
7.  **Add Configuration Files:** Add necessary YAML configuration files under `configs/hardening/` for configuring rules around parallelism, ordering, race probes, shared state, idempotency, queue discipline, and timeouts. (Completed)
8.  **Add Documentation:** Create Markdown guides in `docs/` for architecture, operators, reviewers, references, and maintenance specifically tailored to Post-100 Hardening Pack 03. Update the root `README.md` to reflect these additions. (Completed)
9.  **Write Tests:** Add pytest unit tests for concurrency guards, bounded parallelism, and race probes in the `tests/concurrency_hardening/` directory. (Completed)
10. **Run full test suite**: Explicitly run the full test suite using `pytest` to ensure no regressions were introduced. (Completed)
11. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
12. **Submit:** Submit the changes outlining what was done.

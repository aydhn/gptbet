# AGENT INSTRUCTIONS

We have built Phase 81 Trust Exchange Scale Architecture. The project requires no specific pre-commit hooks setup via `pre-commit` command, but running standard tests using `python -m pytest` indicates the module we worked on acts as expected.
